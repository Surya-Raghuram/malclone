# malclone
 
# MyAnimeList Clone

```bash
# Clone the repo
git clone https://github.com/Sergio-Marq/malclone.git
cd malclone

# Install dependencies
npm install

# Start development server
npm run dev

If the project still doesnt run, do install the missing dependencies
shown in the console error using the commmand:
npm i <module_name> 
